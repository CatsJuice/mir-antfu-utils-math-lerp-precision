# mir-antfu-utils-math-lerp-precision

Minimal issue repo of [antfu/utils](https://github.com/antfu/utils) `lerp` precision issue.

## Steps to reproduce

1. Clone this repo
2. Install dependencies with `ni`
3. Run with `nr test` or `nr dev`